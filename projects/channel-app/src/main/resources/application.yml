server:
  port: 9090

logging:
  level:
    root: info
    #'[org.springframework.web]': ERROR
    #'[br.com.k3t.mvc.oauth]': TRACE

spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: channel-app
            client-secret: dDoAclRfmKoq5Ba4MIPPBYLm95YTprZl
            scope:
              - openid
              - profile
              - email
              - address
        provider:
          keycloak:
            # A barra final é importante!
            issuer-uri: http://localhost:7080/realms/bootcamp-realm